-- Run this in your MySQL client to create the alerts table
-- Usage: mysql -u <user> -p <database> < create_alerts_table.sql

-- If you are using the MySQL CLI, run:
-- mysql -u youruser -p yourdb < create_alerts_table.sql

-- If you want to run it interactively:
-- 1. Log in: mysql -u youruser -p yourdb
-- 2. Paste the contents of create_alerts_table.sql

-- Table definition is in create_alerts_table.sql

-- For further integration (e.g., SIEM webhook, Slack, Teams), let me know your preferred platform and endpoint details.
